{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for JustVoice desktop windows",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "http:default",
    "dialog:default",
    "fs:default",
    "shell:default",
    "process:default"
  ]
}
