# 2026-06-13 — QC batch (post wiring-audit merge)

Queue. Work one item at a time, full read of the touched surface, RULE #1
precedent check, RULE #2 conformance pass before calling each item done.

## V1 — Voices: page scrolls, only voices list should scroll

**Current state (TODO line):** whole `VoicesView.vue` scrolls with the
page. Library list should be its own scroll container inside a
`.jv-fill` page that does not overflow.

**Target:** page is fixed-height (`.jv-fill`); the voices list/cards
section gets `overflow-y:auto`; toolbar + lede stay pinned at top.

## S1 — Studio Cast: relocate smart-assign/clear/add-persona buttons

**Current state:** these buttons live outside the Characters card (per
the user, on a row above/around it). The reference (JustWrite screenshot
the user attached — verbal description only since the picture isn't
visible to this session) puts them *inside* the Characters card header.

**Target:** Characters card owns its own header with the action row
(Smart assign, Clear, + Persona). Match the row pattern from JustWrite
cast screenshots.

## S2 — Studio Cast: Characters card and Voices card same height + scrollable

**Current state:** the two side-by-side cards collapse to their content
heights; one looks taller than the other; nothing scrolls within them
when content overflows.

**Target:** both cards fill the same vertical track. Each card has its
own internal scroller for overflow. Page itself doesn't scroll.

## S3 — Studio: replace top lede copy + bold/cap CAST SCRIPT RENDER EXPORT

**Current state:** lede starts "Studio turns ..." with current copy.

**Target:** replace with:

> Studio turns your written manuscript into a narrated audiobook in
> three sequential steps — choose voices in **CAST**, let the AI work
> out who speaks each line in **SCRIPT**, then generate the audio
> chapter by chapter in **RENDER**, package in **EXPORT**. You can
> write a whole novel without touching it; it exists for writers who
> want to produce their own audiobook or hear their prose read aloud as
> a revision tool.

The four step names are CAPITALIZED and **bold**. Lede sits at the top
of the view.
