# QC round 3 queue — intake 2026-06-12 evening

<!-- SPDX-License-Identifier: GPL-3.0-or-later -->

Execution gated on the user approving the CSS-sweep findings + plan
(item S). Items 1-7 queue per RULE #2 after that approval.

1. **Voice preview bar at bottom — remove** (identify which bar:
   GlobalAudioPlayer persistent bar vs ChapterView floating generate
   bar; user said "get rid of it").
2. **"Pick it in Engines" → link** in the no-engine banner.
3. **Labs tab explainers** — Compare / Train / Audio get an intro lede
   like Speaker Lab's.
4. **Voice inspector → Train LoRA** opens Train with the voice
   preselected in its dropdown (parity with how "Blend with…" pre-fills
   the blend form). Currently: toast + bare #train navigation.
5. **Captures pane padding** — needless scroll (fold into sweep fixes).
6. **Chapters "Open Projects" button does nothing** (empty state).
7. **Chapters header** — big wasted wrapping space (fold into sweep).
S. **Whole-app HTML/CSS consistency sweep** — findings + plan FIRST,
   user approves, then fixes.
